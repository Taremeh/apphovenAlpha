<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- theme to use FOR launch screen-->
    <style name="LaunchScreenThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="toolbarStyle">@style/NativeScriptToolbarStyle</item>

        <item name="colorPrimary">@color/ah_primary</item>
        <item name="colorPrimaryDark">@color/ah_primaryDark</item>
        <item name="colorAccent">@color/ah_accent</item>

        <item name="android:windowBackground">@drawable/splash_screen</item>
        
        <item name="android:windowActionBarOverlay">true</item>  
        <item name="android:windowTranslucentStatus">true</item>

    </style>

    <style name="LaunchScreenTheme" parent="LaunchScreenThemeBase">
    </style>

    <!-- theme to use AFTER launch screen is loaded-->
    <style name="AppThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="toolbarStyle">@style/NativeScriptToolbarStyle</item>

    </style>

    <style name="ApphovenTheme" parent="AppThemeBase">
        <item name="android:statusBarColor">@color/ah_primaryDark</item>
        <item name="android:dividerHeight">0px</item>

        <item name="colorPrimary">@color/ah_primary</item>
        <item name="colorPrimaryDark">@color/ah_primaryDark</item>
        <item name="colorAccent">@color/ah_accent</item>

        <item name="android:textColor">@color/ah_textColor</item>
        <item name="android:textColorPrimary">@color/ah_textColorPrimary</item>
        <item name="android:textColorSecondary">@color/ah_textColorSecondary</item>
    </style>

    <!-- theme for actioon-bar -->
    <style name="NativeScriptToolbarStyleBase" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/ah_primary</item>
        <item name="theme">@style/ThemeOverlay.AppCompat.ActionBar</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat</item>

    </style>

    <style name="NativeScriptToolbarStyle" parent="NativeScriptToolbarStyleBase">
    </style>
</resources> 
